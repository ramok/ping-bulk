# Minimal tmux config for demo recording.
# This file is used via "tmux -f tmux.conf ..." so the user's ~/.tmux.conf
# is completely ignored — ensuring consistent layout regardless of user prefs.

set -g base-index 0
set -g pane-base-index 0
set -g status off
set -g default-terminal "xterm-256color"
